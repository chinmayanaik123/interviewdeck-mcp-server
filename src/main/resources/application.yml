server:
  port: ${PORT:8090}

spring:
  application:
    name: interviewdeck-mcp-server

interviewdeck:
  questions-base-url: https://raw.githubusercontent.com/chinmayannaik/interview-deck-questions/main/
  firebase:
    credentials-path: ${FIREBASE_CREDENTIALS_PATH:config/firebase-service-account.json}
