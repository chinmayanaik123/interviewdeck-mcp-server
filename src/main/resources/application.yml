server:
  port: ${PORT:8090}

spring:
  application:
    name: interviewdeck-mcp-server

interviewdeck:
  questions-base-url: https://raw.githubusercontent.com/chinmayanaik123/interview-helper-question-bank/main/
  firebase:
    credentials-path: ${FIREBASE_CREDENTIALS_PATH:config/firebase-service-account.json}
